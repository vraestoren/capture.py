# capture.py
Mobile-API for [capture](https://play.google.com/store/apps/details?id=com.capture.tech) social network

## Example
```python
from capture import Capture

capture = Capture()
capture.login_with_x_token(x_token="your_x_token")
```
